public class Xiaomi extends Phone {
    public Xiaomi(long SSID, int cameraNumber, int batteryCapacity, boolean amoledDisplay, boolean ipsDisplay, String processorName, String model) {
        super(SSID, cameraNumber, batteryCapacity, amoledDisplay, ipsDisplay, processorName, model);
    }
}